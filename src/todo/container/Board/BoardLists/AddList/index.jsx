import React from 'react';
import PropTypes from 'prop-types';

const AddList = props => {
    return (
        <div>
            Add new list
        </div>
    );
};

AddList.propTypes = {
    
};

export default AddList;