import React from 'react';
import PropTypes from 'prop-types';

const Cards = props => {
    return (
        <div>
            Add and Edit component will appear here
        </div>
    );
};

Cards.propTypes = {
    
};

export default Cards;