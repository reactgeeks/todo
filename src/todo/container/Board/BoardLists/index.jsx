import React from 'react';
import PropTypes from 'prop-types';

const BoardLists = props => {
    return (
        <div>
            Board List will appear here
            will add card component
        </div>
    );
};

BoardLists.propTypes = {
    
};

export default BoardLists;