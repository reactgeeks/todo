import React from 'react';
import PropTypes from 'prop-types';

const Board = props => {
    return (
        <div>
            Board Component
            will add board list component
        </div>
    );
};

Board.propTypes = {
    
};

export default Board;